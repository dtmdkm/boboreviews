{
  "cleanUrls": true,
  "routes": [
    { "src": "/store/(?<slug>[^/]+)", "dest": "/store-page.html?slug=$slug" },
    { "src": "/category/(?<slug>[^/]+)", "dest": "/category.html?slug=$slug" },
    { "src": "/(.*)", "dest": "/$1" }
  ]
}
